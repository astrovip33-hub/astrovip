name: AstroVip Performance Preview Audit

on:
  push:
    branches:
      - "perf/cwv-mobile-desktop-20260922-v1"

permissions:
  contents: read

jobs:
  lighthouse:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Wait for preview alias
        shell: bash
        run: |
          for i in $(seq 1 12); do
            code=$(curl -L -sS -o /dev/null -w '%{http_code}' https://deploy-preview-35--astrovip33.netlify.app/ || true)
            echo "Preview HTTP: $code"
            [ "$code" = "200" ] && exit 0
            sleep 5
          done
          exit 1

      - name: Run Lighthouse mobile and desktop
        shell: bash
        run: |
          CHROME_PATH="$(command -v google-chrome || command -v chromium || command -v chromium-browser)"
          export CHROME_PATH
          echo "Chrome: $CHROME_PATH"
          npx --yes lighthouse@latest https://deploy-preview-35--astrovip33.netlify.app/             --output=json --output-path=/tmp/lh-mobile.json             --only-categories=performance             --chrome-flags="--headless --no-sandbox --disable-dev-shm-usage"
          npx --yes lighthouse@latest https://deploy-preview-35--astrovip33.netlify.app/             --preset=desktop             --output=json --output-path=/tmp/lh-desktop.json             --only-categories=performance             --chrome-flags="--headless --no-sandbox --disable-dev-shm-usage"

      - name: Print scores and Core Web Vitals
        shell: bash
        run: |
          node <<'NODE'
          const fs=require('fs');
          for (const mode of ['mobile','desktop']) {
            const j=JSON.parse(fs.readFileSync('/tmp/lh-'+mode+'.json','utf8'));
            const a=j.audits;
            const score=Math.round(j.categories.performance.score*100);
            console.log('ASTROVIP_'+mode.toUpperCase()+'_SCORE='+score);
            console.log('ASTROVIP_'+mode.toUpperCase()+'_FCP='+a['first-contentful-paint'].displayValue);
            console.log('ASTROVIP_'+mode.toUpperCase()+'_LCP='+a['largest-contentful-paint'].displayValue);
            console.log('ASTROVIP_'+mode.toUpperCase()+'_TBT='+a['total-blocking-time'].displayValue);
            console.log('ASTROVIP_'+mode.toUpperCase()+'_CLS='+a['cumulative-layout-shift'].displayValue);
            console.log('ASTROVIP_'+mode.toUpperCase()+'_SI='+a['speed-index'].displayValue);
          }
          NODE
